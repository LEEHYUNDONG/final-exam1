#include<stdio.h>
#include<stdlib.h>
#include"info.h"
#include"Func.h"

int main()
{
    Loaddata(arr);

    //while(1)
    //{
        input_info(arr, &stnum);

    Savedata(arr, &stnum);

    return(0);


}

